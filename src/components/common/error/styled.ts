import { styled } from 'nativewind';
import { Text, View } from 'react-native';

export const Wrapper = styled(View, 'flex flex-1 bg-gray-200 justify-center items-center');
export const ErrorText = styled(Text, 'text-2xl text-red-700');
